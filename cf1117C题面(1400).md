# C. 监视河狸

每次测试的时间限制：2 秒  
每次测试的内存限制：256 兆字节  

给定一棵有 \( n \) 个顶点的有根树，顶点编号为 \( 1 \) 到 \( n \)。顶点 \( 1 \) 是树的根。河狸初始时位于根，它沿着树行进到位于顶点 \( a_1, \ldots, a_m \) 的某个河狸水坝处。

你需要确定河狸最终去了这 \( m \) 个顶点中的哪一个。为此，你可以在树的任意边上安装摄像头。如果河狸经过一条装有摄像头的边，你将会观察到这一情况。为清晰起见，我们假设在河狸所有移动结束后，你将获得一个装有摄像头的边的序列，其中记录了河狸经过相应边时被观测到的顺序。

由于摄像头价格昂贵，必须使用足够唯一确定河狸目的地的最少数量的摄像头。你需要给出所需的最少摄像头数量 \( k \)，以及应该安装在这些摄像头的位置（即对应的边）。

---

*有根树是指其中一个顶点被指定为根的特殊树。*

## 输入

每个测试包含多个测试用例。第一行包含测试用例数 \( t \)（\( 1 \leq t \leq 2 \cdot 10^4 \)）。接下来是每个测试用例的描述。

每个测试用例的第一行包含一个整数 \( n \)（\( 2 \leq n \leq 10^5 \)）。

第二行包含 \( n - 1 \) 个整数 \( p_2, \ldots, p_n \) —— 顶点 \( 2 \) 到 \( n \) 的父节点（\( 1 \leq p_i < i; \; 2 \leq i \leq n \)）。

第三行包含一个整数 \( m \) —— 含有河狸水坝的顶点数量（\( 1 \leq m \leq n \)）。

第四行包含 \( m \) 个整数 \( a_1, \ldots, a_m \) —— 这些顶点的编号（\( 1 \leq a_i < n; \; 1 \leq i \leq m \)）。所有 \( a_i \) 互不相同。

保证所有测试用例的 \( n \) 之和不超过 \( 10^5 \)。

## 输出

对于每个测试用例，输出恰好一行。首先输出一个整数 \( k \) —— 所需的最少摄像头数量，然后在该行中，对于每条需要安装摄像头的边（连接顶点 \( u \) 和 \( p_u \)），输出顶点编号 \( u \)。

如果存在多个答案，你可以输出任意一个。