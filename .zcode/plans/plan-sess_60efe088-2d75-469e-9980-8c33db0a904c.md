Update `/mnt/c/users/57178/Desktop/母项目/OpenDoggo/AGENTS.md` (via targeted Edits, not a rewrite) so it records the newly staged s04 hooks reference lesson, per the user instruction "阅读理解references s4项目的内容，记录到agent.md".

Findings driving the update: `references/` now stages the s04 "Hooks" lesson (`README.zh.md` title: "s04: Hooks — 挂在循环上，不写进循环里"; `references/code.py` lines 126–208 hold the hook system; images are now `hooks-overview*.svg`). The Java source is still at s03 state — `AgentLoop` calls `permissionChecker.check()` directly — so AGENTS.md's "currently the s03 lesson" framing is stale.

Edits:

1. **Project Boundary, line 5** — change "currently the s03 permission lesson" to the s04 hooks lesson; note s02/s03 code survives inside `references/code.py` under the "From s02-s03" section and that `references/images/` now holds `hooks-overview*.svg` (permission SVGs deleted).

2. **Project Boundary, line 7** — after "s03 is complete…", add that s04 (hooks) is staged in `references/` but not yet implemented in Java.

3. **s03 section, line 29** — replace the "Next lesson: s04 hooks…" bullet with a pointer saying s04 moves the hardcoded `check()` call onto a PreToolUse hook (details in the new s04 section).

4. **Insert a new `## s04 — Hooks (staged in references/, not yet implemented)` section** between the s03 section and `## Commands`, recording:
   - Thesis: extensions hang on the loop instead of being written into it; the loop only calls `trigger_hooks()`.
   - Registry mechanics: `HOOKS` = event → ordered callback list; `register_hook`/`trigger_hooks`; callbacks run in registration order; first non-None result short-circuits.
   - The four events and per-event semantics: `UserPromptSubmit` (fires in REPL before the query enters history; return ignored), `PreToolUse` (per tool call, replaces the hardcoded check; non-None string blocks that call and becomes the `tool_result` content for the original `tool_use_id`; sibling calls still run), `PostToolUse` (after execution, before result append; return ignored), `Stop` (before loop exit when no tool calls; non-None string is appended as a user message and the loop continues).
   - The five reference hooks (`permission_hook`, `log_hook`, `large_output_hook` >100,000 chars, `context_inject_hook`, `summary_hook`) and why registration order matters (permission before log, so denied calls never log).
   - Scope facts: no new tools, no hook config files, synchronous in-process callbacks, no error handling around hook calls.
   - Reference-vs-Java deltas to preserve when porting: reference deny list has 6 entries (Java has 7, extra `> /dev/sda`); reference lacks Java's bash outside-workspace rule; Java should keep its `ApprovalPrompt` abstraction instead of console polling inside the hook; reference denial strings are specific and become the tool_result content (vs Java's generic "Permission denied.").
   - Java integration points: Stop wraps AgentLoop's empty-toolCalls branch; PreToolUse replaces the `permissionChecker.check(toolCall)` call; PostToolUse between `toolDispatch.execute` and the result append; UserPromptSubmit in `Main`'s REPL before history append.

5. Re-read the final file to confirm coherence, then summarize the changes for the user.