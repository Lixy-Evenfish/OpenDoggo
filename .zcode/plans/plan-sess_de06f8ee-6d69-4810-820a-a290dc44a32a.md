更新 `/mnt/c/users/57178/Desktop/母项目/OpenDoggo/AGENTS.md`（已确认 `.zcode/`、`.agents/` 下无指令文件）。用 Edit 逐段更新，不做整体重写。文件保持现有风格：英文正文 + 中文课题原句引用。共 5 处修改：

## 1. Project Boundary（修正过时状态）
- 第 5 行：references/ 现在是 **s05 TodoWrite 课题**（s02–s04 代码保留在 code.py 的 "From s02-s04" 段；images/ 现为 `todo-overview*.svg`；新增 `references/example/hello.py` 是 README 试一试提示的演示目标文件）。
- 第 7 行：s04 hooks **已随 v2.0 提交实现**（`io.opendoggo.hook` 包）；s05 已暂存未实现。

## 2. Current Architecture（反映 hook 化后的真实架构）
- `Main`：wiring + REPL + `initTools`/`initHooks` 注册；失败回滚不变。
- `AgentLoop` 改写为"静默核心"：构造器 `(ModelClient, HookRunner, ToolDispatch)`，无 PermissionChecker 引用、无控制台输出；每次执行前 trigger PreToolUse（非 null 字符串 → `ToolResult(toolCall.id(), blocked)` 跳过执行，兄弟调用仍运行），执行后 PostToolUse；空 toolCalls 分支 trigger Stop（非 null → 追加为 user 消息继续循环）。所有控制台输出都在 hook 里。
- `PermissionChecker`：作为 Main.initHooks 注册的**第一个 PreToolUse hook** 被调用（`permissionChecker::check`），三道门不变；拒绝字符串为 "Permission denied by deny list" / "Permission denied by user"（不再是泛化的 "Permission denied."）。
- 审批 UI 在新类 `permission.ConsoleApprovalPrompt`（共享 REPL 的 BufferedReader），Main 装配。
- 注明 README.md 架构树滞后（缺 hook 包和 ConsoleApprovalPrompt）。

## 3. s03 段（小修历史指针）
- 头部注明"自 v2.0 起门检查经 PreToolUse 权限 hook 运行"；console 实现位置改为 `ConsoleApprovalPrompt`；拒绝内容改为具体字符串；末行 "s04 moves…" 改为"已迁移，见 s04 段"。

## 4. s04 段改题为 "s04 — Hooks (implemented in v2.0)"，重写为实现现状
- `HookRunner`：每事件一个有序列表 + 四个 `@FunctionalInterface`；PreToolUse/Stop 首个非 null 短路返回，UserPromptSubmit/PostToolUse 为 void。
- 四个触发点（Main REPL 在 query 入 history 前；AgentLoop 执行前/后/空 toolCalls 分支）。
- `Main.initHooks` 按序注册 7 个 hook：context-inject → permission → banner（`[HOOK-PreToolUse]> name`）→ log → 全量输出打印（注：200 字符预览已被全量输出取代）→ large-output（>100,000）→ Stop summary；顺序意义：permission 在 banner/log 之前，被拒调用不打印。
- 保留的 reference-vs-Java 差异：Gate 1 七条（多 `> /dev/sda`）、Java 独有 bash 出工作区规则、`ApprovalPrompt` 抽象。

## 5. 新增 "## s05 — TodoWrite (staged in `references/`, not yet implemented)" 段（本次核心，用户要求记录 s05↔s04 差异以指导后续 Java 完善）
- 课题论点：`todo_write` 增加的是**规划能力**而非执行能力（"没有计划的 agent 走哪算哪"；长任务漂移因为工具结果稀释系统提示影响力）。
- **相对 s04 的变更表**：工具 5→6（+todo_write）；SYSTEM 提示加入"先计划再执行"引导；循环加入 `rounds_since_todo` 计数与 reminder 注入；hook 体系（4 事件 5 钩子）与全部数值限制（120s、50000、100000、200 glob、max_tokens 8000）**完全不变**。
- `TodoManager`（code.py 115–171 行）：内存 `items`，**整表替换**；`update()` 解析链 `json.loads` → `ast.literal_eval`（绝不用 eval）；校验：list、≤20 条、`content` 非空、`status` ∈ pending/in_progress/completed（默认 pending）、**至多一个 in_progress**；返回 `render()`（`[ ]`/`[>]`/`[x]` + `(done/total completed)`）。
- `run_todo_write`：ValueError → 工具结果 "Error: {e}"（校验错误回传模型不崩溃）；成功时向控制台打印黄色 "## Current Tasks" 并返回渲染列表；经普通 handler 表分发，无特殊化；schema 与运行时限制镜像（maxItems 20 等）。
- **催更 reminder 是普通循环状态而非 hook**：连续 3 个含工具调用的轮次没调 todo_write → 向该轮 results 追加 `{"type":"text","text":"<reminder>Update your todos.</reminder>"}`（与 tool_results 同一条 user 消息），计数清零；轮内只要调过 todo_write 即清零。
- 附带两处循环加固：handler 调用包 try/except → "Error: {e}"；tool_result 内容经 `str()` 规整。
- `references/example/hello.py` 用途说明；下一课 s06 Subagent 一句带过。
- **Java 集成点**：新建 `TodoManager` + `TodoWriteTool`（实现 ToolHandler，Main.initTools 注册）；AgentLoop.run 加计数器与 reminder 注入；系统提示加规划句；`toolDispatch.execute` 包 try/catch 返回 "Error: {e}"。设计约束：保持工具无 UI——"## Current Tasks" 控制台打印应放 PostToolUse hook 或 Main 侧，不进工具内部（延续 s04 的分层原则）。

Commands 与 Configuration and Safety 两段核实后无需改动（`mvn compile`/`test`/`package`、无 src/test 文件、.env 规则均仍准确）。

完成后向用户总结各段改动并给出文件路径。