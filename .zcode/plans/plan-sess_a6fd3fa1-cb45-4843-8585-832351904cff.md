# 更新 AGENTS.md：记录 s06 Subagent 参考材料相对 s05 的增量

只改一个文件：根目录 `AGENTS.md`（已存在，用 Edit 增量更新，不整体重写）。不实现 s06 的 Java 代码——本次仅按用户要求"记录 s06 比 s05 新增的部分，以便后续完善 Java"。`.zcode/`、`.agents/` 下无 AGENTS.md，无冲突。

## 前置发现（将写入文件的核心事实）

- `references/` 已整体换为 s06 课程：README 三语版、`code.py`（独立 s06 清单）、`images/subagent-overview*.svg`；s05 时代的 `example/hello.py`、`todo-overview*.svg` 已删除（均未提交）。
- **s06 的 code.py 不含 s05 的 todo_write**：课程 code.py 是逐课独立清单，s06 = s02–s04 基础（5 个基础工具 + 权限即 hook + hook 系统）+ `task`。Java 仓库是累积式（现有 6 工具），移植时 `task` 将是第 7 个。

## 具体编辑（4 处）

1. **Project Boundary 第 1 条重写**：references/ 现为 s06 Subagent 课（列出文件名与图片名），说明"逐课独立、s06 不带 todo_write、Java 累积式"这一差异；完成状态句补一句 "s06 reference staged, Java port pending"。

2. **s05 节末条修正过期文件引用**：`references/example/hello.py` 已随 s06 staged 而移除，改为过去时说明。

3. **新增 `## s06 — Subagent (reference staged, Java port pending)` 节**（插在 s05 节与 Commands 之间，沿用现有小节风格）：
   - 主旨：委派——给子任务一段独立上下文（课标题 "s06: Subagent — 给子任务一段独立上下文"）；子代理从全新 `messages[]` 开始，只有最终文本返回父循环。
   - 参考代码相对 s05 的新增清单（code.py 行号引用）：`SUB_SYSTEM` 提示词（"Complete the given task, then return a concise final answer."）、父 SYSTEM 换句（"Use task for focused exploration or a self-contained subtask." 替代 s05 的 todo 句）、`extract_text`、`run_subagent`（30 轮上限、Stop hook 作用于子消息列表、`(no summary)` 兜底、超限返回字符串 "Subagent stopped after 30 turns without a final answer." 而非崩溃）、`TASK_TOOL` schema（prompt/required/minLength 1）、注册方式 `TOOLS=[*BASE_TOOLS, TASK_TOOL]`；`SUB_TOOLS`/`SUB_HANDLERS` 无 task → 只允许一层委派；控制台 UX（品红 `[Subagent started/done/stopped]`、灰色 `[sub] name: 前100字符`）；数值限制全部不变，唯一新数字是 30 轮上限。
   - 边界表（README）：对话=全新 messages；执行=同进程同 WORKDIR；返回=仅最终文本；委派深度=一层；工具策略=父子共享 hooks（同一权限门 + Gate 3 审批）。
   - **Java 映射笔记与扩展点**（后续移植的落点）：`ModelResponse.text()` 已等价 `extract_text`；`AgentLoop.run` 已有 Stop 强制续跑语义；三个需适配点——(a) system prompt 固化在 `AnthropicClient` 构造器（Main.java:72），参考代码是每次 create 传 `system=`，需要第二个 client 或按调用传参；(b) `MAX_TOOL_ROUNDS=50` 超限抛 `IllegalStateException`，子循环需 30 轮上限且返回哨兵字符串；(c) `ToolDispatch` 无受限视图，自然做法是为子循环单独注册仅含基础工具的第二个 dispatch。
   - **待定决策清单**（明确标记未决）：子代理工具集是严格五基础工具（参考）还是含 `todo_write`（累积式 Java）；若子循环复用 `AgentLoop` 而其 dispatch 无 todo_write，`roundsSinceTodo` 会在子循环里每 3 轮注入 reminder（参考无此行为）——保留/抑制/参数化需定；`[sub]` 预览打印与现有全量输出 PostToolUse hook 的重复打印如何取舍；`task` 工具大概率无需权限规则（内层调用仍过门），移植时确认。
   - 试跑提示：README 的三个 try-it prompt；`references/example/` 已不存在，移植时需自建目标材料；下一课 s07 Skill Loading。

4. **修正过期说法**：Current Architecture 末条提到根 `README.md`——该文件在工作区已删除（未提交），改为如实描述。

Commands、Configuration and Safety 两节不动（无新命令；TodoDemo 仍有效）。

## 验证

纯文档改动，无需 `mvn compile`；改完重读 AGENTS.md 检查前后一致、无残留 todo-overview/hello.py 引用。